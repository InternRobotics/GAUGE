public/world-model-results/**/*.mp4 filter=lfs diff=lfs merge=lfs -text
# GitHub Pages does not hydrate LFS pointers, so its generated site copy must
# contain the actual video bytes while the source media above stays in LFS.
world-model-results/**/*.mp4 -filter -diff -merge -text
